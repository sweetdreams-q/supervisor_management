import 'package:flutter/material.dart';

class BrowseStaffScreen extends StatelessWidget {
  const BrowseStaffScreen({super.key});

  @override
  Widget build(BuildContext context) => const SizedBox.shrink();
}
